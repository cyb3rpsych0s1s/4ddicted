set windows-shell := ["powershell.exe", "-NoLogo", "-Command"]
set dotenv-load
set fallback

mod      := 'Addicted'
source   := absolute_path(join('..', '..', 'audioware'))
tree     := 'mods'

@install TO LOCALE:
    just dir '{{ join(TO, tree, mod) }}';
    just dir '{{ join(TO, tree, mod, LOCALE) }}';
    just dir '{{ join(TO, tree, mod, "vanilla", LOCALE) }}';
    just dcopy '{{ join(source, LOCALE) }}' '{{ join(TO, mod, LOCALE) }}'
    just dcopy '{{ join(source, "vanilla", LOCALE) }}' '{{ join(TO, mod, "vanilla", LOCALE) }}'
    just copy '{{ join(source, "voices.yml") }}' '{{ join(TO, mod, "voices.yml") }}'

@uninstall FROM:
    just trash '{{ join(FROM, tree, mod) }}'